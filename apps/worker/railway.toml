[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm db:generate && pnpm --filter @repo/worker build"

[deploy]
startCommand = "pnpm --filter @repo/worker start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
